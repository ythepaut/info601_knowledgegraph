package model.node;

import model.Property;

import java.util.HashMap;

public class ConceptNode extends Node {
    public ConceptNode(HashMap<String, Property<?>> properties) {
        super(properties);
    }
}
